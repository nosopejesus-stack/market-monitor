__pycache__/
*.pyc
.pytest_cache/
# customer documents and pipeline outputs must never be committed
samples/
runs/
*.jsonl
.env

# market data from Twelve Data: not redistributed (provider terms); re-pull per quant/README.md
quant/data/*.csv

# PLACSP downloads and client job folders (tender pliegos, client profiles) never go in git
placsp/
jobs/
